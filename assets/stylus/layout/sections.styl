+component(wrapper) {
    margin:    0 auto;
    max-width: 1200px;
    width:     90%;
}

+component(section) {
    min-height: 600px;
    padding:    1rem 0 5rem 0;

    +above($breakpoints.tablet) {
        padding: 1.5rem 0 3rem 0;
    }
}
