import _ from "lodash";

class Parallax {
    /**
     * Create a new Parallax instance.
     *
     * @param  {Array}
     * @return {Parallax}
     */
    constructor(options) {
        var defaults = {
            opacity:    false,
            resistance: 1,
            inverse:    true
        };
    }
}

export default Parallax;