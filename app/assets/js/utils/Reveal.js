class Reveal {
    /**
     * Create a new Reveal instance.
     * @param  {object}  element
     * @return {Reveal}
     */
    constructor(element) {
        this.element = element;
    }
}

export default Reveal;